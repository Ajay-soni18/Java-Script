"use strict"  // it will treat as all the code new version.

//alert(3+3) it will work in browser console here we are using nodejs

// data types =>
// number , string , boolean ,null (object) , undefined
let ajay;
console.log(typeof ""); // typeof will tell us the datatype
console.log(typeof null); // data type of null is object
console.log(typeof ajay); // if varible is not intialised , datatype will be undefined

// **********************************datatypes**********************************************
// mainly categorized into two primitive and non primitive(reference)
// primitive :- string , number , boolean , null , symbol , BigInt. they all sent by value 
// referance(non-primitive) :-array , objects , functions

//JavaScript is a dynamically typed language. This means that variable types are determined at runtime, 
//and you do not need to explicitly declare the type of a variable before using it. 
//You can assign different types of values to a variable during its lifetime.

const bigvalue = 8282222222222232292838n; // to convert in bigInt add n at the end it will store a BigINT